# Rapport banc web SUTA — 2026-09-02 16:56 UTC

**Mission** : mesurer l'écoute et les réponses vocales de SUTA en production (https://suta-bot-web.vercel.app), suite au signalement de Patrick (« la quantité d'écoute et de réponses vocales a beaucoup baissé »).

## Constat principal : aucune mesure n'a pu être effectuée

L'environnement d'exécution de cette session (conteneur distant Claude Code) **bloque tout accès sortant vers `suta-bot-web.vercel.app`** au niveau du proxy de sortie réseau. Le blocage intervient dès l'établissement de la connexion TLS (CONNECT refusé), avant toute requête HTTP.

Tentatives effectuées (4 au total, aucune n'a atteint le site) :

| # | Heure (UTC) | Méthode | Résultat exact |
|---|---|---|---|
| 1 | 16:55:42 | `curl GET /` via proxy | `HTTP 000`, exit 56 — proxy : `connect_rejected — gateway answered 403 to CONNECT (policy denial or upstream failure)` pour `suta-bot-web.vercel.app:443` |
| 2 | 16:55 (+3 s) | `curl GET /` (retry 1) | Identique : `connect_rejected` ×1 |
| 3 | 16:55 (+8 s) | `curl GET /` (retry 2) | Identique : `connect_rejected` ×1 |
| 4 | 16:56 | Outil WebFetch (chemin réseau distinct) | `{"error_type":"EGRESS_BLOCKED","domain":"suta-bot-web.vercel.app","message":"Access to suta-bot-web.vercel.app is blocked by the network egress proxy."}` |

## Conséquences sur chaque étape prévue

- **Étape 1 (version pied de page, /api/health)** : non mesurable — la connexion est refusée avant la réponse HTTP. Version pied de page : non relevée. JSON de `/api/health` (dont `rechercheCorpus`) : non relevé.
- **Étape 2 (3 sondes texte `/api/tools/search-knowledge`)** : non exécutées — même blocage. Aucune latence ni nombre de résultats relevés.
- **Étape 3 (banc vocal `evals/suta/vocal-qa/run.mjs`)** : non lancé. Le banc charge la même URL dans Chromium avant d'établir la session WebRTC ; le site étant inaccessible au niveau TCP/TLS depuis ce conteneur, le banc échouerait au chargement de page (avant même le test du média UDP). Lancer `npm install` puis le banc n'aurait produit qu'un échec certain et redondant ; conformément à la consigne (« ne pas insister au-delà de 2 tentatives »), les tentatives se sont arrêtées après confirmation du blocage sur deux chemins réseau distincts.

## Éléments de contexte relevés (dépôt local, lecture seule)

- Dépôt cloné sur la branche `feature/suta-experience`, dernier commit `e905b94` (« Revue du 03/09 : les demandes larges reçoivent l'accueil qui oriente ») — soit exactement la version attendue en pied de page (`v-e905b94`).
- Le banc vocal (`run.mjs`, `scenarios.mjs`, `personas.mjs`, `campagne.mjs`) est présent dans le dépôt et n'a pas été modifié.

## Fait inattendu à noter

Le blocage réseau semble provenir de la **politique de sortie de l'environnement Claude Code distant** (réponse 403 du proxy au CONNECT, qualifiée « organization policy »), et non du site lui-même : rien ne permet de dire que le site est en panne. Ce point est à vérifier côté configuration de l'environnement (politique réseau de l'environnement d'exécution, qui doit autoriser `suta-bot-web.vercel.app` — et pour le banc vocal, également les domaines Azure OpenAI Realtime et le média WebRTC/UDP).

## Conclusion (sur la question posée)

La question « l'écoute et les réponses vocales ont-elles baissé ? » **reste sans réponse mesurée** : aucune requête n'a pu atteindre la production depuis ce conteneur, le proxy de sortie refusant toute connexion vers `suta-bot-web.vercel.app` (403 au CONNECT, confirmé sur deux chemins réseau distincts). Aucun indice recueilli ici ne permet donc ni de confirmer ni d'infirmer une baisse de l'écoute ou des réponses. Prochaine mesure possible : relancer exactement ce protocole depuis un environnement dont la politique réseau autorise `suta-bot-web.vercel.app` (et le média WebRTC pour l'étape vocale).
