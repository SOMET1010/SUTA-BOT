# packages/tools

Réservé au **Lot 5** (tool calling) : outils appelables par SUTA via
function calling, à commencer par `searchKnowledge`, puis `get_program`,
`get_service`, `get_procedure`, `get_contact`, `find_office`, `get_faq`,
`get_event`, etc. Les outils d'écriture (`create_request`,
`schedule_appointment`, ...) ne seront pas activés dans le MVP Salon.

Non implémenté dans ce commit. Voir cahier des charges, sections 17-18.
