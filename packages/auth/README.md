# packages/auth

Réservé aux lots suivants : authentification Microsoft Entra ID, gestion
des niveaux de visibilité (`PUBLIC`, `DEMO`, puis `AUTHENTICATED`,
`EMPLOYEE`, `MANAGER`, `ADMIN`). Pour le MVP Salon, seuls `PUBLIC` et
`DEMO` sont utilisés côté `packages/knowledge`.

Non implémenté dans ce commit. Voir cahier des charges, sections 19 et 57.
