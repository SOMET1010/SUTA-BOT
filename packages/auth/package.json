{
  "name": "@suta/auth",
  "version": "0.1.0",
  "private": true,
  "description": "Contrôle d'accès minimal à l'administration SUTA (pas encore l'authentification Entra ID, section 57)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^3.2.4"
  }
}
